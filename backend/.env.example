HF_TOKEN=hf_xxx

# Optional, defaults to redis://localhost:6379
# REDIS_URL=redis://localhost:6379

# Optional, defaults to http://localhost:5173 (Vite dev server) — the only
# origin allowed to call the API (CORS). Set to the frontend's real URL in
# production.
# FRONTEND_ORIGIN=http://localhost:5173
